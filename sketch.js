let x = 300;
let y = 300;
let radius = 150;

function setup() {
  createCanvas(600, 600);
  background(145, 89, 78);
  noStroke();
  fill(10, 20, 30);
  circle(x, y, radius);
  square(x, y, radius);
}
